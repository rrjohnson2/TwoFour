import { CountTimePipe } from './count-time.pipe';

describe('CountTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CountTimePipe();
    expect(pipe).toBeTruthy();
  });
});
